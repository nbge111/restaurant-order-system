# 前端环境变量
# 部署到 Vercel 时需要将此处的 your-render-backend 替换为实际的 Render 后端地址

# 开发环境（本地后端）
VITE_API_URL=http://localhost:3001/api

# 生产环境（部署后取消注释并修改为你的 Render 地址）
# VITE_API_URL=https://restaurant-order-api.onrender.com/api
